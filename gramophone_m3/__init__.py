"""Gramophone M3: full-textbook scale + live interactive sessions.

Merges chapter v1 graphs into namespaced book graphs and plays beat graphs
live (stdin or scripted attempts) with retry plus save/resume. Reuses M1
(spacing) and M2 (validators, adapt, srs, xapi) — no duplicated logic.
"""

__version__ = "0.3.0"
